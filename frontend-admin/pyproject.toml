[project]
name = "maintainer-copilot-admin"
version = "0.1.0"
description = "Maintainer's Copilot — Streamlit admin app"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.40.0",
    "requests>=2.32.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]